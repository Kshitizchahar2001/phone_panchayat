// ignore_for_file: file_names

import 'package:flutter/material.dart';

abstract class AttachedMedia extends StatelessWidget {
  // const AttachedMedia({Key key}) : super(key: key);
  const AttachedMedia({
    Key key,
  }) : super(key: key);
}
